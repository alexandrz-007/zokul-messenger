# Prompt: Executor Implement Active Task

```text
Use project-executor.
Implement docs/tasks/active/NEXT_AGENT_TASK.md exactly as specified.
Do not expand scope.
Run required verification, update worklog and task execution result, then commit only intentional files if the task asks for a commit.
```
