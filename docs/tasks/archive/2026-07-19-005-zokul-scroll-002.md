# Next Agent Task: ZOKUL-DOCS-001 - Sync docs to production reality (2026-07-19)

Protocol version: 1.0
Task type: documentation (no product code)
Execution owner: current agent (executor under governor)
Created by: project-governor
State required before execution: Ready for Execution

## Goal

Bring `docs/` in line with the real project state as of 2026-07-19: read receipts and
scroll-fix are in production; `feature/pwa-proper` is NOT merged (killer PWA is production
default); Cloudflare tunnel is OFF; deploy pipeline is `prepare-release.ps1 -SkipChecks`
-> `zokul-deploy` -> push `master:production` -> server `git reset --hard` + `docker compose
up -d --build`. Archive the completed scroll-002 artifacts so `active/` is not stale.

## Current Reality (source of truth)

- `master` HEAD: 9f54824 (merge of feature/scroll-fix).
- production (GitHub `production`): 9897dd5 (scroll-fix). Prior: e52812f (read receipts).
- Client tests: 26/26 (incl. 2 scroll + 2 message-read). Server tests: 78/78.
- PWA: `client/sw.ts` == killer (`sw.kill.ts`). `feature/pwa-proper` has `sw.kill.ts`/`sw.pwa.ts`
  + `scripts/select-sw.mjs` + npm scripts `sw:kill`/`sw:proper`; NOT merged.
- Cloudflare tunnel (ZOKUL-NET-001) OFF per user; direct 443 + killer PWA only.
- Deploy pipeline: `scripts/prepare-release.ps1 -SkipChecks` copies tree to `D:\zokul-deploy`,
  preserving `ssl/` and `.env`; push `master:production`; server `git fetch && git reset --hard
  origin/production && docker compose -f docker-compose.prod.yml up -d --build`.

## Allowed Files

- `docs/CONTROL_PLANE.md`
- `docs/PROJECT_HEALTH.md`
- `docs/BACKLOG.md`
- `docs/DEPLOYMENT.md`
- `docs/ARCHITECTURE.md`
- `docs/PROJECT_BRIEF.md`
- `docs/ROADMAP.md`
- `docs/tasks/active/NEXT_AGENT_TASK.md` (replace with "no active task" stub)
- `docs/reviews/active/NEXT_REVIEW.md` (archive)
- `docs/tasks/active/` (archive current review/task)
- `docs/AI_WORKLOG.md` (append one entry about this docs sync)

## Forbidden Files

- `client/**`, `server/**` (product code)
- `sw.*`, `vite.config.ts`, `nginx.conf`, `docker-compose*.yml`, `scripts/prepare-release.ps1`
- `.env`, `ssl/`, secrets, production data

## Must Do

- [ ] Archive `docs/reviews/active/NEXT_REVIEW.md` ->
      `docs/reviews/archive/2026-07-19-005-zokul-scroll-002-review.md`
- [ ] Archive `docs/tasks/active/NEXT_AGENT_TASK.md` ->
      `docs/tasks/archive/2026-07-19-005-zokul-scroll-002.md`
- [ ] Replace `docs/tasks/active/NEXT_AGENT_TASK.md` with a stub:
      "No active task. Last completed: ZOKUL-SCROLL-001 (reaffirmed, in production 9897dd5)."
- [ ] `CONTROL_PLANE.md`:
      - State -> `Ready for Planning` (Idle, no active task).
      - Active task -> `None`; Active review -> `None`.
      - Remove "awaiting deploy / next: deploy read receipts" language.
      - Recently Completed: add "Read receipts (ZOKUL-READ-001/002) in production (e52812f)";
        "Auto-scroll fix (ZOKUL-SCROLL-001) in production (9897dd5)".
      - Next Required Action: manual Safari/iPhone read-receipts verification (deferred);
        optional future merge of `feature/pwa-proper` after Safari stability.
      - Keep Notes: pwa-proper separate/not merged; Cloudflare OFF.
- [ ] `PROJECT_HEALTH.md`:
      - "Last reviewed: 2026-07-19"; "Source commit: 9f54824".
      - Verification: Build Passed (2026-07-19); Tests Passed (client 26/26, server 78/78,
        2026-07-19); Docker build Passed (local + prod). Remove stale 19/19 / 71/71 rows.
      - Active Task -> None. Remove "Next Action: fix scroll jump".
- [ ] `BACKLOG.md`:
      - "Last reviewed: 2026-07-19"; Source commit 9f54824.
      - Add under appropriate sections:
        - [x] ZOKUL-READ-001 message_reads backend + chat:read socket (Accepted, in prod)
        - [x] ZOKUL-READ-002 read receipts frontend (Accepted, in prod)
        - [x] ZOKUL-SCROLL-001 auto-scroll fix on chat switch (Accepted, in prod 9897dd5)
      - Update any test-count references (78 server / 26 client).
- [ ] `DEPLOYMENT.md`:
      - "Last reviewed: 2026-07-19"; Source commit 9f54824.
      - Add "## Production Release Pipeline" describing the real flow:
        `prepare-release.ps1 -SkipChecks` -> `D:\zokul-deploy` (preserves ssl/, .env) ->
        `git push master:production` -> on server `git fetch && git reset --hard
        origin/production && docker compose -f docker-compose.prod.yml up -d --build`,
        plus pre-reset safety `git check-ignore ssl .env` and `curl /api/health` check.
      - Add "## PWA Strategy": killer `sw.ts` = production default; `feature/pwa-proper`
        split (sw.kill.ts/sw.pwa.ts + select-sw.mjs); NOT merged; Cloudflare OFF per user.
- [ ] `ARCHITECTURE.md`, `PROJECT_BRIEF.md`, `ROADMAP.md` (SELECTIVE):
      - Update "Last reviewed" -> 2026-07-19 and "Source commit" -> 9f54824.
      - Only fix factual contradictions (branches/deploy/PWA/read receipts). Do NOT expand scope.
- [ ] `AI_WORKLOG.md`: append one dated entry summarizing ZOKUL-DOCS-001.

## Must Not Do

- [ ] Do NOT edit product code, PWA source, deploy scripts, compose files, secrets.
- [ ] Do NOT merge `feature/pwa-proper`.
- [ ] Do NOT change test counts by re-running/rewriting tests (only document existing 78/78 + 26/26).

## Acceptance Criteria

- [ ] No stale "awaiting deploy" / "next: deploy read receipts" in CONTROL_PLANE.
- [ ] Active task/review archived; `tasks/active/NEXT_AGENT_TASK.md` is a no-task stub.
- [ ] DEPLOYMENT.md documents the real `prepare-release.ps1` -> zokul-deploy -> production flow + PWA strategy.
- [ ] Test counts in docs reflect 78 server / 26 client; production = 9897dd5.
- [ ] All changed docs carry updated "Last reviewed" / source commit.
- [ ] `git status` shows only docs/ changes (no product code).

## Test Requirements

| Type | Required | Scope |
| --- | --- | --- |
| Docs consistency (manual) | yes | cross-check CONTROL_PLANE/HEALTH/BACKLOG/DEPLOYMENT vs git log/production |
| Build/test | Not applicable | docs-only task |

## Verification Commands

| Command | Required | Expected |
| --- | --- | --- |
| `git status --short` | yes | only docs/ modifications |
| `git log --oneline -1` (master) / `git ls-remote` production | yes | master=9f54824; production=9897dd5 (already deployed) |

## Documentation Updates Required

- [ ] All listed in Must Do.

## Stop Conditions

- If a doc contradicts code in a way that needs code change (not doc fix), stop and ask governor.

## Notes

- This task is docs-only. No protocol state changes beyond archiving + Idle control plane.
- Branch: stay on `master`; commit as "docs: sync to production reality (ZOKUL-DOCS-001)".
